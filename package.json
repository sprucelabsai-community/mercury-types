{
  "name": "@sprucelabs/mercury-types",
  "publishConfig": {
    "access": "public"
  },
  "version": "26.1.1001",
  "files": [
    "build"
  ],
  "skill": {
    "namespace": "mercury",
    "commandOverrides": {
      "sync.events": "--shouldSyncOnlyCoreEvents true --skillEventContractTypesFile ../../types/mercury.types --eventBuilderFile ../../../utilities"
    },
    "upgradeIgnoreList": [
      "globby",
      "@sprucelabs/spruce-test-fixtures",
      "@sprucelabs/spruce-skill-utils"
    ]
  },
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "module": "./build/esm/index.js",
  "sideEffects": false,
  "description": "Types for Mercury!",
  "keywords": [
    "node",
    "components",
    "sprucebot",
    "sprucelabs"
  ],
  "homepage": "https://github.com/sprucelabsai/mercury-workspace",
  "bugs": {
    "url": "https://github.com/sprucelabsai/mercury-workspace/issues"
  },
  "scripts": {
    "build.copy-files": "mkdir -p build && rsync -avzq --exclude='*.ts' ./src/ ./build/",
    "build.ci": "yarn build.tsc && yarn build.resolve-paths && yarn lint",
    "build.dev": "yarn build.tsc --sourceMap ; yarn resolve-paths.lint",
    "build.dist": "yarn clean && yarn build.tsc && yarn build.resolve-paths && yarn build.esm",
    "build.esm": "tsc --project tsconfig.dist.json && yarn build.resolve-paths.esm && yarn build.esm-postbuild",
    "build.esm-postbuild": "esm-postbuild --target esm --patterns '**/*.js'",
    "build.resolve-paths": "resolve-path-aliases --target build --patterns '**/*.js,**/*.d.ts'",
    "build.resolve-paths.esm": "TS_NODE_PROJECT=`pwd`/tsconfig.dist.json resolve-path-aliases --target build/esm --patterns '**/*.js,**/*.d.ts'",
    "build.tsc": "yarn build.copy-files && tsc",
    "build.types": "tsc --emitDeclarationOnly && echo PASS",
    "clean": "yarn clean.build",
    "clean.all": "yarn clean.dependencies && yarn clean.build",
    "clean.build": "rm -rf build/",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock",
    "fix.lint": "eslint --fix --cache '**/*.ts'",
    "lint": "eslint --cache '**/*.ts'",
    "rebuild": "yarn clean.all && yarn && yarn build.dev",
    "release": "semantic-release",
    "test": "jest",
    "update.dependencies": "yarn clean.dependencies && yarn",
    "upgrade.packages": "yarn-upgrade-all && rm -f yarn.lock ; yarn ; yarn fix.lint ; true",
    "upgrade.packages.all": "yarn install && yarn upgrade.packages",
    "upgrade.packages.test": "yarn upgrade.packages.all && yarn lint && yarn build.dev && yarn test",
    "watch.lint": "concurrently 'yarn lint' \"chokidar 'src/**/*' -c 'yarn lint.tsc'\"",
    "watch.tsc": "tsc -w",
    "post.watch.build": "yarn build.copy-files && yarn build.resolve-paths",
    "watch.build.dev": "tsc-watch --sourceMap --onCompilationComplete 'yarn post.watch.build'",
    "lint.tsc": "tsc -p . --noEmit",
    "resolve-paths.lint": "yarn build.resolve-paths ; yarn lint",
    "watch.rebuild": "yarn clean.all && yarn && yarn watch.build.dev"
  },
  "dependencies": {
    "@sprucelabs/error": "^5.0.319",
    "@sprucelabs/schema": "^25.4.74",
    "@sprucelabs/spruce-core-schemas": "^22.6.258"
  },
  "devDependencies": {
    "@sprucelabs/esm-postbuild": "^1.0.298",
    "@sprucelabs/jest-json-reporter": "^6.0.267",
    "@sprucelabs/resolve-path-aliases": "^1.0.237",
    "@sprucelabs/semantic-release": "^4.0.8",
    "@sprucelabs/test": "^7.7.249",
    "@sprucelabs/test-utils": "^3.0.305",
    "@types/node": "^16.11.12",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^6.4.0",
    "conventional-changelog-sprucelabs": "^1.1.2",
    "eslint": "^8.4.1",
    "eslint-config-spruce": "^10.10.16",
    "jest": "^27.4.3",
    "jest-circus": "^27.4.2",
    "prettier": "^2.5.1",
    "ts-node": "^10.4.0",
    "tsc-watch": "^4.5.0",
    "tsconfig-paths": "^3.12.0",
    "typescript": "^4.5.2"
  },
  "jest": {
    "maxWorkers": 4,
    "testTimeout": 120000,
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/tmp/",
      "<rootDir>/src/",
      "<rootDir>/node_modules/",
      "<rootDir>/build/__tests__/testDirsAndFiles/",
      "<rootDir>/build/esm/"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js?(x)"
    ],
    "moduleNameMapper": {
      "^#spruce/(.*)$": "<rootDir>/.spruce/$1"
    }
  }
}
